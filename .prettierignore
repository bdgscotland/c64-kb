# Markdown carries metadata lines, tables and listings the extractor and the
# listing gate read; a reflow would change what they see.
*.md
docs/
templates/
dist/
data/
storage/
.tools/
package-lock.json

# Assembly and other listings: Prettier parses .inc as HTML and fails (CI
# run on 89af00a, demos/measured/test/stub.inc).
*.inc
*.asm
*.s
*.a65
