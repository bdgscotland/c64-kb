# Security

Report a vulnerability through GitHub's private reporting: the repository's
**Security** tab, then **Report a vulnerability**. Do not open a public issue.

In scope: the CLI and the MCP server in `src/`, the scripts in `scripts/`,
and the Claude Code hooks in `.claude/hooks/`. A wrong fact in `docs/` is not
a security issue; open an ordinary issue for it.
