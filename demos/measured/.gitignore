build/
local.mk
.DS_Store
*.log
*.sym
# shots/pal.png and shots/ntsc.png are the committed end-screen pins.
# Everything else verify.sh writes there is regenerated on every run.
shots/*
!shots/pal.png
!shots/ntsc.png
