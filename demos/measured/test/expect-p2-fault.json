{
  "note": "Part 2 built with -define FORCE_FAULT: the depth table is complemented, so the pass gives the farthest ball sprite 0 and the nearest sprite 7. check.py requires a verdict check and its verdict type passes only on green; this build is red by design (selfcheck returns 0), so verify.sh expects exactly those two verdict rows to fail (one for PAL, one for NTSC). The other assertions check the visible fault (grey over white in the ball overlap) and that the column is unchanged. NTSC note: after the half-copy fix the top 60 band lines carry phase 42 on NTSC at frame 300; the column band-line-0 check is PAL-only for the same reason as in expect-p2.json. The same check covers only the bottom half (rasters 147-214) where both models agree.",
  "checks": [
    { "name": "verdict (expected FAIL: this build grades itself red by design)", "type": "verdict" },

    { "name": "border is red: the stub's FAIL colour", "type": "pixel", "x": 2, "y": 100, "colour": 2 },
    { "name": "border is red at the right edge too", "type": "pixel", "x": 380, "y": 100, "colour": 2 },

    {
      "name": "ball 0 (far, grey) drawn OVER ball 1 (near, white) at the overlap's middle",
      "type": "pixel",
      "vic_x": 228,
      "line": 152,
      "colour": 12
    },
    {
      "name": "ball 0 over ball 1 at the overlap's left, lower half",
      "type": "pixel",
      "vic_x": 226,
      "line": 155,
      "colour": 12
    },
    { "name": "ball 0 over ball 1, lower half", "type": "pixel", "vic_x": 230, "line": 156, "colour": 12 },
    { "name": "ball 0 over ball 2 (near, white)", "type": "pixel", "vic_x": 220, "line": 152, "colour": 12 },
    {
      "name": "ball 1's white still shows beyond ball 0's small image",
      "type": "pixel",
      "vic_x": 244,
      "line": 152,
      "colour": 1
    },

    {
      "name": "the column is unchanged: band line 0 first lit x 132 (PAL: top half at phase 43)",
      "type": "pixel",
      "vic_x": 156,
      "line": 87,
      "colour": 1,
      "models": ["pal"]
    },
    {
      "name": "the column is unchanged: band line 100 last lit x 156",
      "type": "pixel",
      "vic_x": 180,
      "line": 187,
      "colour": 1
    },

    {
      "name": "bottom half of the band (rasters 147-214) is the same on PAL and NTSC",
      "type": "same",
      "area": { "vic_x": 120, "line": 147, "width": 136, "height": 68 }
    }
  ]
}
