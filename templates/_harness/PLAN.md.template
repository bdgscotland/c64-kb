# Plan

Fill this in before any code. `make` and the Claude Code hook refuse to
build or write under `src/` while a `FILL:` line is left, or while the
Compatibility and Budget sections lack the tools' own output.

Run the tools in the c64-kb checkout: `cd $C64KB && npx tsx src/cli.ts <command>`.

## Concept

FILL: two or three sentences. What the player sees and does. PAL, NTSC or both.

## Briefing

Command: `npx tsx src/cli.ts game-briefing "<concept>" --archetype <name>`
(`demo-briefing` for a demo).

FILL: the parts of the briefing you are acting on: techniques kept, techniques
dropped and why, the pitfalls it named that this program meets.

## Techniques

| Technique | Why | Recipe it starts from | Pitfalls read (`pitfalls-for`) |
|---|---|---|---|
| FILL: name | | | |

## Compatibility

Command: `npx tsx src/cli.ts check-compatibility <technique> <technique> ...`

```text
FILL: paste the whole output here. It starts "# Compatibility:".
```

What the warnings mean for this program:

FILL: one line per warning, or "none".

## Budget

Command: `npx tsx src/cli.ts plan-budget <technique> ... --region both [--sprites n --sprite-lines n]`

```text
FILL: paste the whole output here. It starts "# Budget plan:".
```

The frame meter measures the real figure once the loop runs; record it here
with its model and the command (`make shot check` prints it).

FILL: what is unknown in the budget and how the meter will settle it.

## Memory and screen

FILL: where code, the KickAssembler blob, sprites, character set, screen and
music live. In AUTOPILOT builds the meter owns screen row 24, columns 20 to 39,
in the power-on character set; CIA2 timer A belongs to the meter.

## Autopilot and checks

FILL: the scripted input, the verdict the program grades itself on ($02FF and
the border), and the expect.json checks that grade the screenshots.

## Decisions and open questions

FILL: anything settled on the way, with its evidence; anything still open.
